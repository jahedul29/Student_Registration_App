@model IEnumerable<Student>
@{
    ViewData["Title"] = "Index";
}

<h3 class="text-info">Student List</h3>

<a asp-action="Register" class="btn btn-lg btn-success">Register</a>

@if (Model.Count() > 0)
{

}
else
{
    <div>No student. Register new student.</div>
}
